import React, { FC } from "react";
import { StyledContent } from "./style";

const Content: FC = () => {
   return (
      <StyledContent>
         Content
      </StyledContent>
   );
}

export default Content;