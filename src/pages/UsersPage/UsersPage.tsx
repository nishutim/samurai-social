import React, { FC } from "react";
import { Page, PageBody } from "../../components/styled/Page";

const UsersPage: FC = () => {
   return (
      <Page>
         <PageBody>
            Users
         </PageBody>
      </Page>
   );
}

export default UsersPage;