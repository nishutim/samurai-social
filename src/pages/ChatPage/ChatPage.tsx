import React, { FC } from "react";
import { Page, PageBody } from "../../components/styled/Page";

const ChatPage: FC = () => {
   return (
      <Page>
         <PageBody>
            Chat
         </PageBody>
      </Page>
   );
}

export default ChatPage;